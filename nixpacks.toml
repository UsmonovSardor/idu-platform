[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["cd idu-backend && npm ci --omit=dev"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "node idu-backend/server.js"
